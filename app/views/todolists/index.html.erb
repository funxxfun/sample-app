<h1>投稿一覧</h1>
<% @lists.each do |list| %>
  <P>タイトル</P>
  <span>
    <%= link_to list.title, "/todolists/#{list.id}" %>
  </span>
  <P>画像</P>
  <%= attachment_image_tag list, :image, size:"100x100",format:'jpeg' %>
<% end %>